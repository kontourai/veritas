{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://veritas.dev/schemas/veritas-evidence.schema.json",
  "title": "Veritas Evidence Record",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "framework_version",
    "run_id",
    "timestamp",
    "source_ref",
    "source_kind",
    "source_scope",
    "resolved_phase",
    "resolved_workstream",
    "matched_artifacts",
    "affected_nodes",
    "affected_lanes",
    "selected_proof_commands",
    "selected_proof_lanes",
    "proof_resolution_source",
    "uncovered_path_result",
    "baseline_ci_fast_passed",
    "recommendations",
    "false_positive_review",
    "promotion_candidate",
    "override_or_bypass",
    "files",
    "unresolved_files",
    "promotion_allowed",
    "surface",
    "framework",
    "adapter",
    "policy_pack",
    "policy_results"
  ],
  "properties": {
    "framework_version": { "type": "integer", "minimum": 1, "x_surface_mapping": "veritas-local" },
    "run_id": { "type": "string", "x_surface_mapping": "mapped", "x_surface_targets": ["metadata", "report-input"] },
    "timestamp": { "type": "string", "x_surface_mapping": "mapped", "x_surface_targets": ["claim", "evidence", "event"] },
    "source_ref": { "type": "string", "x_surface_mapping": "mapped", "x_surface_targets": ["claim", "evidence", "metadata"] },
    "source_kind": {
      "type": "string",
      "enum": ["explicit-files", "branch-diff", "working-tree"],
      "x_surface_mapping": "mapped",
      "x_surface_targets": ["evidence", "metadata"]
    },
    "source_scope": { "$ref": "#/$defs/stringArray", "x_surface_mapping": "mapped", "x_surface_targets": ["evidence", "metadata"] },
    "resolved_phase": { "type": "string", "x_surface_mapping": "mapped", "x_surface_targets": ["claim", "metadata"] },
    "resolved_workstream": { "type": "string", "x_surface_mapping": "mapped", "x_surface_targets": ["claim", "metadata"] },
    "matched_artifacts": { "$ref": "#/$defs/stringArray", "x_surface_mapping": "mapped", "x_surface_targets": ["metadata"] },
    "affected_nodes": { "$ref": "#/$defs/stringArray", "x_surface_mapping": "mapped", "x_surface_targets": ["claim", "evidence", "event"] },
    "affected_lanes": { "$ref": "#/$defs/stringArray", "x_surface_mapping": "mapped", "x_surface_targets": ["metadata"] },
    "selected_proof_commands": { "$ref": "#/$defs/stringArray", "x_surface_mapping": "mapped", "x_surface_targets": ["claim", "evidence"] },
    "selected_proof_lanes": {
      "type": "array",
      "items": { "$ref": "#/$defs/proofLane" },
      "x_surface_mapping": "mapped",
      "x_surface_targets": ["claim", "evidence", "policy", "event"]
    },
    "proof_resolution_source": { "type": "string", "x_surface_mapping": "mapped", "x_surface_targets": ["metadata"] },
    "proof_family_results": {
      "type": "array",
      "items": { "$ref": "#/$defs/proofFamilyResult" },
      "x_surface_mapping": "mapped",
      "x_surface_targets": ["claim", "evidence", "policy", "event", "metadata"]
    },
    "verification_budget": { "$ref": "#/$defs/verificationBudget", "x_surface_mapping": "mapped", "x_surface_targets": ["claim", "evidence", "event", "metadata", "report-input"] },
    "uncovered_path_result": {
      "type": "string",
      "enum": ["clear", "ignore", "warn", "fail"],
      "x_surface_mapping": "mapped",
      "x_surface_targets": ["metadata"]
    },
    "baseline_ci_fast_passed": { "type": ["boolean", "null"], "x_surface_mapping": "mapped", "x_surface_targets": ["claim", "event", "metadata"] },
    "recommendations": {
      "type": "array",
      "items": { "type": "object" },
      "x_surface_mapping": "mapped",
      "x_surface_targets": ["metadata"]
    },
    "false_positive_review": { "$ref": "#/$defs/reviewState", "x_surface_mapping": "mapped", "x_surface_targets": ["metadata"] },
    "promotion_candidate": { "type": "boolean", "x_surface_mapping": "mapped", "x_surface_targets": ["metadata"] },
    "override_or_bypass": { "type": "boolean", "x_surface_mapping": "mapped", "x_surface_targets": ["metadata"] },
    "owner": { "type": ["string", "null"], "x_surface_mapping": "mapped", "x_surface_targets": ["metadata"] },
    "files": { "$ref": "#/$defs/stringArray", "x_surface_mapping": "mapped", "x_surface_targets": ["evidence", "metadata"] },
    "unresolved_files": { "$ref": "#/$defs/stringArray", "x_surface_mapping": "mapped", "x_surface_targets": ["evidence", "metadata"] },
    "promotion_allowed": { "type": "boolean", "x_surface_mapping": "mapped", "x_surface_targets": ["metadata"] },
    "surface": { "$ref": "#/$defs/surfaceBlock", "x_surface_mapping": "mapped", "x_surface_targets": ["report-input"] },
    "framework": {
      "type": "object",
      "x_surface_mapping": "veritas-local",
      "additionalProperties": false,
      "required": ["version", "resolver_precedence", "policy_defaults"],
      "properties": {
        "version": { "type": "integer", "minimum": 1 },
        "resolver_precedence": { "$ref": "#/$defs/stringArray" },
        "policy_defaults": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "false_positive_review",
            "promotion_candidate",
            "override_or_bypass"
          ],
          "properties": {
            "false_positive_review": { "$ref": "#/$defs/reviewState" },
            "promotion_candidate": { "type": "boolean" },
            "override_or_bypass": { "type": "boolean" }
          }
        }
      }
    },
    "adapter": {
      "type": "object",
      "x_surface_mapping": "veritas-local",
      "additionalProperties": false,
      "required": [
        "name",
        "kind",
        "report_transport",
        "default_resolution",
        "non_sliceable_invariants",
        "proof_lanes",
        "required_proof_lane_ids",
        "default_proof_lane_ids",
        "surface_proof_lanes",
        "uncovered_path_policy"
      ],
      "properties": {
        "name": { "type": "string" },
        "kind": { "type": "string" },
        "report_transport": { "type": "string" },
        "default_resolution": { "$ref": "#/$defs/resolution" },
        "non_sliceable_invariants": { "$ref": "#/$defs/stringArray" },
        "proof_lanes": {
          "type": "array",
          "items": { "$ref": "#/$defs/proofLane" }
        },
        "required_proof_lane_ids": { "$ref": "#/$defs/stringArray" },
        "default_proof_lane_ids": { "$ref": "#/$defs/stringArray" },
        "surface_proof_lanes": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["node_ids", "proof_lane_ids"],
            "properties": {
              "node_ids": { "$ref": "#/$defs/stringArray" },
              "proof_lane_ids": { "$ref": "#/$defs/stringArray" }
            }
          }
        },
        "uncovered_path_policy": {
          "type": "string",
          "enum": ["ignore", "warn", "fail"]
        }
      }
    },
    "policy_pack": {
      "type": "object",
      "x_surface_mapping": "mapped",
      "x_surface_targets": ["claim", "metadata"],
      "additionalProperties": false,
      "required": ["name", "version", "rule_count"],
      "properties": {
        "name": { "type": "string" },
        "version": { "type": "integer", "minimum": 1 },
        "rule_count": { "type": "integer", "minimum": 0 }
      }
    },
    "policy_results": {
      "type": "array",
      "items": { "$ref": "#/$defs/policyResult" },
      "x_surface_mapping": "mapped",
      "x_surface_targets": ["claim", "evidence", "event", "metadata", "report-input"]
    }
  },
  "$defs": {
    "stringArray": {
      "type": "array",
      "items": { "type": "string" }
    },
    "surfaceBlock": {
      "type": "object",
      "additionalProperties": false,
      "required": ["input"],
      "properties": {
        "input": { "$ref": "#/$defs/surfaceTrustInput" }
      }
    },
    "surfaceTrustInput": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schemaVersion", "source", "claims", "evidence", "policies", "events"],
      "properties": {
        "schemaVersion": { "const": 2 },
        "source": { "type": "string" },
        "claims": {
          "type": "array",
          "items": { "type": "object" }
        },
        "evidence": {
          "type": "array",
          "items": { "type": "object" }
        },
        "policies": {
          "type": "array",
          "items": { "type": "object" }
        },
        "events": {
          "type": "array",
          "items": { "type": "object" }
        }
      },
      "not": {
        "anyOf": [
          { "required": ["id"] },
          { "required": ["generatedAt"] },
          { "required": ["summary"] },
          { "required": ["faultLines"] },
          { "required": ["proofRequirementsByClaimId"] }
        ]
      }
    },
    "reviewState": {
      "type": "string",
      "enum": ["unknown", "accepted", "rejected"]
    },
    "proofLane": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "command", "method", "surface_claim_ids", "summary"],
      "properties": {
        "id": { "type": "string" },
        "command": { "type": "string" },
        "method": {
          "enum": ["observation", "extraction", "validation", "corroboration", "attestation", "auditability", "anchoring", "monitoring"]
        },
        "surface_claim_ids": { "$ref": "#/$defs/stringArray" },
        "summary": { "type": "string" }
      }
    },
    "proofFamilyResult": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "lane_id",
        "source_proof_lane_id",
        "manifest_path",
        "destination",
        "owner",
        "disposition",
        "blocking_status",
        "verification_weight",
        "selected",
        "recent_catch_evidence",
        "regression_severity",
        "false_positive_risk",
        "replacement_test_available",
        "review_trigger",
        "last_reviewed",
        "evidence_basis",
        "freshness_status",
        "rationale"
      ],
      "properties": {
        "id": { "type": "string" },
        "lane_id": { "type": "string" },
        "source_proof_lane_id": { "type": ["string", "null"] },
        "manifest_path": { "type": "string" },
        "destination": { "type": ["string", "null"] },
        "owner": { "type": ["string", "null"] },
        "disposition": {
          "type": "string",
          "enum": ["required", "candidate", "advisory", "move-to-test", "retire", "upstream-abstraction"]
        },
        "blocking_status": { "type": "string" },
        "verification_weight": {
          "type": "string",
          "enum": ["blocking", "advisory", "informational"]
        },
        "selected": { "type": "boolean" },
        "recent_catch_evidence": { "type": "string" },
        "regression_severity": { "type": "string" },
        "false_positive_risk": { "type": "string" },
        "replacement_test_available": { "type": ["string", "null"] },
        "review_trigger": { "type": ["string", "null"] },
        "last_reviewed": { "type": ["string", "null"] },
        "evidence_basis": { "type": "string" },
        "freshness_status": {
          "type": "string",
          "enum": ["current", "review-needed", "stale", "retiring"]
        },
        "rationale": { "type": "string" }
      }
    },
    "verificationBudget": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "proof_lane_count",
        "selected_proof_lane_count",
        "proof_family_count",
        "required_family_count",
        "candidate_family_count",
        "advisory_family_count",
        "move_to_test_family_count",
        "retire_family_count",
        "upstream_candidate_count",
        "unknown_catch_evidence_family_ids",
        "missing_review_trigger_family_ids",
        "stale_family_ids",
        "stale_or_unknown_family_ids",
        "recommendation"
      ],
      "properties": {
        "proof_lane_count": { "type": "integer", "minimum": 0 },
        "selected_proof_lane_count": { "type": "integer", "minimum": 0 },
        "proof_family_count": { "type": "integer", "minimum": 0 },
        "required_family_count": { "type": "integer", "minimum": 0 },
        "candidate_family_count": { "type": "integer", "minimum": 0 },
        "advisory_family_count": { "type": "integer", "minimum": 0 },
        "move_to_test_family_count": { "type": "integer", "minimum": 0 },
        "retire_family_count": { "type": "integer", "minimum": 0 },
        "upstream_candidate_count": { "type": "integer", "minimum": 0 },
        "unknown_catch_evidence_family_ids": { "$ref": "#/$defs/stringArray" },
        "missing_review_trigger_family_ids": { "$ref": "#/$defs/stringArray" },
        "stale_family_ids": { "$ref": "#/$defs/stringArray" },
        "stale_or_unknown_family_ids": { "$ref": "#/$defs/stringArray" },
        "recommendation": { "type": "string" }
      }
    },
    "resolution": {
      "type": "object",
      "additionalProperties": false,
      "required": ["phase", "workstream", "matchedArtifacts"],
      "properties": {
        "phase": { "type": "string" },
        "workstream": { "type": "string" },
        "matchedArtifacts": { "$ref": "#/$defs/stringArray" }
      }
    },
    "policyResult": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "rule_id",
        "classification",
        "stage",
        "message",
        "owner",
        "rollback_switch",
        "implemented",
        "passed",
        "summary",
        "findings"
      ],
      "properties": {
        "rule_id": { "type": "string" },
        "classification": { "type": "string" },
        "stage": { "type": "string" },
        "message": { "type": "string" },
        "owner": { "type": ["string", "null"] },
        "rollback_switch": { "type": ["string", "null"] },
        "implemented": { "type": "boolean" },
        "passed": { "type": ["boolean", "null"] },
        "summary": { "type": "string" },
        "findings": {
          "type": "array",
          "items": { "type": "object" }
        }
      }
    }
  }
}
