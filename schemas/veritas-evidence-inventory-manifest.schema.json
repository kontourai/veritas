{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://veritas.dev/schemas/veritas-evidence-inventory-manifest.schema.json",
  "title": "Veritas Evidence Inventory Manifest",
  "type": "object",
  "additionalProperties": true,
  "required": ["items"],
  "properties": {
    "version": { "type": "integer", "minimum": 1 },
    "sourceEvidenceCheckId": { "type": "string" },
    "generatedFrom": { "type": "string" },
    "migrationStage": { "type": "string" },
    "reviewPolicy": {
      "type": "object",
      "additionalProperties": true
    },
    "items": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/evidenceInventory" }
    }
  },
  "$defs": {
    "evidenceInventory": {
      "type": "object",
      "additionalProperties": true,
      "required": ["id"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "evidenceCheckId": { "type": "string", "minLength": 1 },
        "destination": { "type": "string" },
        "owner": { "type": "string" },
        "defaultDisposition": {
          "type": "string",
          "enum": ["required", "candidate", "advisory", "move-to-test", "retire", "upstream-abstraction"]
        },
        "disposition": {
          "type": "string",
          "enum": ["required", "candidate", "advisory", "move-to-test", "retire", "upstream-abstraction"]
        },
        "currentBlockingStatus": { "type": "string" },
        "recentCatchEvidence": { "type": "string" },
        "regressionSeverity": { "type": "string" },
        "falsePositiveRisk": { "type": "string" },
        "replacementTestAvailable": { "type": "string" },
        "expiryOrReviewTrigger": { "type": "string" },
        "lastReviewed": { "type": "string" },
        "evidenceBasis": { "type": "string" },
        "freshnessStatus": {
          "type": "string",
          "enum": ["current", "review-needed", "stale", "retiring"]
        },
        "rationale": { "type": "string" }
      }
    }
  }
}
