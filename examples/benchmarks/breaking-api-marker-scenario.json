{
  "version": 1,
  "id": "breaking-api-marker",
  "title": "Breaking API marker surfaces on the tagged response",
  "description": "The marker becomes relevant when a public contract changes. The benchmark passes only when the tagged assistant response explicitly surfaces the backward-compatibility risk without surfacing it earlier.",
  "marker": {
    "id": "must-mention-breaking-api",
    "required_phrases": ["breaking API change", "backward compatibility"]
  },
  "scoring": {
    "trigger_tag": "marker-trigger",
    "response_tag": "marker-response-window",
    "max_assistant_turns_after_trigger": 1,
    "allow_early": false
  }
}
